## PYTHON 前言

##物件語法
     物件.方法   pd.Dataframe()
     物件.屬性  iris.data
     
##方法呼叫
     回傳值變數=方法()     X= load_iris() 
     
##參數傳入方法多個參數傳入 多個回傳值傳出
    回傳值變數y1,y2,y3=方法(x1,x2,x3,x4)  

X_train, X_test, y_train, y_test = train_test_split(iris.data, iris.target,test_size=0.3, random_state= 12)

##資料存取
  資料名稱[起始值:結束值]     iris[0:5]
